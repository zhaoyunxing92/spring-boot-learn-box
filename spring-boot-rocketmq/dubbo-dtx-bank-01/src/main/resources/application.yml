spring:
  application:
    name: dubbo-dtx-bank-01
  profiles:
    active: local
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
dubbo:
  scan:
    base-packages: io.github.xyz.spring.boot.rocketmq.controller
  protocol:
    name: dubbo
    port: -1